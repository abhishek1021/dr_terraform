# Terraform Backend Configuration
# This backend is created by the bootstrap process

terraform {
  backend "s3" {
    # These values will be provided during terraform init
    # or via backend config files
  }
}
